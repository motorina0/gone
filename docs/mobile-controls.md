# Mobile controls

The canvas uses pointer events, `touch-action: none`, full-bleed ENVELOP scaling, safe-area padding, 100dvh, and controls at least 44px square. A short stationary contact moves the operative, a double-tap runs, and movement over the threshold pans without issuing an order. Two pointers step through the five zoom levels with a pinch. Desktop additionally supports crop-aware edge scrolling, accelerated arrow-key panning, and wheel zoom anchored under the pointer. Hover previews the complete route on ordinary maps; hazard-dense maps retain immediate move/blocked cursor feedback and calculate the route only when clicked, avoiding repeated main-thread path searches during pointer motion. Tactical level 1 is the widest view whose unobscured rectangle remains fully inside the exact map footprint; no tactical level exposes backdrop beyond the authored map. Level 5 keeps the whole operative inside the usable height and locks camera follow. The same semantic level survives perspective and orientation changes; SAT remains an independent adaptive overview. The compact portrait dock keeps all essential controls visible, labeled for assistive technology, and keyboard-focusable. Exploration pauses when the document is hidden.
