# Mobile controls

The canvas uses pointer events, `touch-action: none`, full-bleed ENVELOP scaling, safe-area padding, 100dvh, and controls at least 44px square. A short stationary contact moves the operative, a double-tap runs, and movement over the threshold pans without issuing an order. Two pointers pinch around their midpoint. Desktop additionally supports crop-aware edge scrolling, accelerated arrow-key panning, and wheel zoom anchored under the pointer. Hover previews the complete route on ordinary maps; hazard-dense maps retain immediate move/blocked cursor feedback and calculate the route only when clicked, avoiding repeated main-thread path searches during pointer motion. Tactical projections open at 3× but can zoom out until the complete map fits the unobscured screen area; SAT opens at that adaptive overview. Follow mode is available as a labeled button and automatically disengages on manual panning. The compact portrait dock keeps all essential controls visible, labeled for assistive technology, and keyboard-focusable. Exploration pauses when the document is hidden.
