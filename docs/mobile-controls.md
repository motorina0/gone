# Mobile controls

The canvas uses pointer events, `touch-action: none`, responsive FIT scaling, safe-area padding, 100dvh, and at least 44px controls. A short stationary contact is a tap; movement over the threshold pans and cannot issue a move. Portrait shows a non-blocking landscape suggestion. The simulation pauses when the document is hidden.
