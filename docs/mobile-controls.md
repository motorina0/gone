# Mobile controls

The canvas uses pointer events, `touch-action: none`, full-bleed ENVELOP scaling, safe-area padding, 100dvh, and controls at least 44px square. A short stationary contact moves the operative, a double-tap runs, and movement over the threshold pans without issuing an order. Two pointers pinch around their midpoint. Desktop additionally supports crop-aware edge scrolling, accelerated arrow-key panning, cursor route preview, and wheel zoom anchored under the pointer. SAT automatically fits the complete map to the visible screen area; tactical projections return to their remembered 3×-or-higher framing. Follow mode is available as a labeled button and automatically disengages on manual panning. The compact portrait dock keeps all essential controls visible, labeled for assistive technology, and keyboard-focusable. Exploration pauses when the document is hidden.
