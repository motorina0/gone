import type {Phase} from '../core/types'; import type {Settings} from '../persistence/settings'; import './style.css';
export interface HudState{selected:boolean;objective:boolean;cooldown:number;alert:string;takedown:boolean;phase:Phase;settings:Settings}
export const createHud=(root:HTMLElement,actions:{pause:()=>void;restart:()=>void;sound:()=>void;takedown:()=>void;settings:(key:keyof Settings,value:number|boolean)=>void})=>{root.innerHTML=`<header><b>SHADOW GRID</b><span id="version">build ${__BUILD_VERSION__}</span></header><aside id="status"></aside><section class="actions"><button id="sound">Distraction [Q]</button><button id="takedown">Silent takedown [E]</button><button id="pause">Pause [P]</button><button id="restart">Restart</button></section><details><summary>Settings & controls</summary><label>Volume <input id="volume" type="range" min="0" max="1" step=".1"></label><label><input id="rotation" type="checkbox"> Camera rotation</label><label><input id="cones" type="checkbox"> Vision cones</label><p>Left-click operative, then ground to move. Middle/right drag pans or rotates. Wheel zooms. Q distracts nearby guards; E performs a takedown from behind. Reach the violet intel zone, then the green extraction.</p></details><div id="overlay" role="dialog" aria-live="assertive"></div>`;const $=<T extends HTMLElement>(s:string)=>root.querySelector<T>(s)!;$('#sound').onclick=actions.sound;$('#takedown').onclick=actions.takedown;$('#pause').onclick=actions.pause;$('#restart').onclick=actions.restart;$('#volume').oninput=e=>actions.settings('volume',Number((e.target as HTMLInputElement).value));$('#rotation').onchange=e=>actions.settings('cameraRotation',(e.target as HTMLInputElement).checked);$('#cones').onchange=e=>actions.settings('visionCones',(e.target as HTMLInputElement).checked);return{render(s:HudState){$('#status').innerHTML=`<strong>${s.selected?'Operative selected':'Select your operative'}</strong><span>Objective: ${s.objective?'Intel secured — extract!':'Secure the violet intel cache'}</span><span>Distraction: ${s.cooldown>0?s.cooldown.toFixed(1)+'s':'READY'}</span><span>Threat: ${s.alert}</span>`;const sound=$<HTMLButtonElement>('#sound'),take=$<HTMLButtonElement>('#takedown');sound.disabled=s.cooldown>0||s.phase!=='playing';take.disabled=!s.takedown;take.classList.toggle('ready',s.takedown);$<HTMLButtonElement>('#pause').textContent=s.phase==='paused'?'Resume [P]':'Pause [P]';$<HTMLInputElement>('#volume').value=String(s.settings.volume);$<HTMLInputElement>('#rotation').checked=s.settings.cameraRotation;$<HTMLInputElement>('#cones').checked=s.settings.visionCones;$('#overlay').innerHTML=s.phase==='won'?'<div><h1>Mission complete</h1><p>Intel extracted without a trace.</p><button id="again">Play again</button></div>':s.phase==='lost'?'<div><h1>Operative detected</h1><p>The perimeter is locked down.</p><button id="again">Restart</button></div>':s.phase==='paused'?'<div><h1>Paused</h1></div>':'';$('#overlay').classList.toggle('show',s.phase!=='playing');root.querySelector<HTMLButtonElement>('#again')?.addEventListener('click',actions.restart)}}};
