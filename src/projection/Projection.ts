import type {Projection,ProjectionDefinition} from './ProjectionTypes';
export const createProjection=(definition:ProjectionDefinition):Projection=>{const [a,b,c,d]=definition.matrix;if([a,b,c,d].some(v=>v===undefined))throw new Error(`Invalid projection ${definition.id}`);const determinant=a!*d!-b!*c!;if(Math.abs(determinant)<1e-8)throw new Error(`Non-invertible projection ${definition.id}`);return{worldToScreen(point){return{x:definition.origin.x+(a!*point.x+b!*point.y)*definition.scale,y:definition.origin.y+(c!*point.x+d!*point.y)*definition.scale-point.elevation*definition.scale}},screenToWorld(point,elevation=0){const sx=(point.x-definition.origin.x)/definition.scale,sy=(point.y-definition.origin.y+elevation*definition.scale)/definition.scale;return{x:(d!*sx-b!*sy)/determinant,y:(-c!*sx+a!*sy)/determinant,elevation}}}};
