import Phaser from 'phaser';import {APP_HEIGHT,APP_WIDTH} from './AppConfig';import {BootScene} from '../scenes/BootScene';import {GameScene} from '../scenes/GameScene';import {showError} from '../ui/ErrorOverlay';
export class GameApp{readonly game:Phaser.Game;constructor(locationId:string){this.game=new Phaser.Game({type:Phaser.AUTO,parent:'game',width:APP_WIDTH,height:APP_HEIGHT,backgroundColor:'#101820',pixelArt:false,scale:{mode:Phaser.Scale.ENVELOP,autoCenter:Phaser.Scale.CENTER_BOTH,width:APP_WIDTH,height:APP_HEIGHT},scene:[new BootScene(locationId),GameScene]});this.game.events.on('boot-error',showError)}}
