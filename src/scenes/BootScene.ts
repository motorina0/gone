import Phaser from 'phaser';import {loadLocation} from '../content/ContentLoader';
export class BootScene extends Phaser.Scene{constructor(private locationId:string){super('boot')}create():void{void this.boot()}private async boot():Promise<void>{try{const content=await loadLocation(this.locationId);this.registry.set('content',content);const sprites=['agent-isometric','agent-top','guard-isometric','guard-top','courier-isometric','courier-top','recipient-isometric','recipient-top','civilian-isometric','civilian-top','package'];content.views.forEach((url,index)=>this.load.image(`background-${index}`,url));content.occlusion.forEach((url,index)=>this.load.image(`occlusion-${index}`,url));content.detailOverlays.forEach((url,index)=>this.load.image(`cathedral-${index}`,url));for(const name of sprites)this.load.image(name,new URL(`sprites/${name}.svg`,content.baseUrl).href);this.load.once(Phaser.Loader.Events.COMPLETE,()=>this.scene.start('game'));this.load.start()}catch(error){this.game.events.emit('boot-error',error)}}}
