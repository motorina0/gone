import {ALERT_MEMORY_SECONDS,IDLE_SECONDS,INVESTIGATE_SECONDS,RETURN_SECONDS,SUSPICIOUS_SECONDS} from './config';import type {GuardData} from '../core/types';
export type Signal='none'|'see'|'heard'|'lost';
export const transitionGuard=(g:GuardData,signal:Signal,dt:number):GuardData=>{if(g.defeated)return g;let state=g.state,stateTime=g.stateTime+dt;if(signal==='see'){state='alert';stateTime=0}else if(signal==='heard'&&state!=='alert'){state='suspicious';stateTime=0}else if(state==='suspicious'&&stateTime>SUSPICIOUS_SECONDS){state='investigate';stateTime=0}else if(state==='investigate'&&stateTime>INVESTIGATE_SECONDS){state='return';stateTime=0}else if(state==='alert'&&signal==='lost'&&stateTime>ALERT_MEMORY_SECONDS){state='return';stateTime=0}else if(state==='return'&&stateTime>RETURN_SECONDS){state='patrol';stateTime=0}else if(state==='idle'&&stateTime>IDLE_SECONDS){state='patrol';stateTime=0}return {...g,state,stateTime}};
