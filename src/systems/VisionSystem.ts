import type {Rect} from '../content/ContentTypes';import type {EntityState,WorldPoint} from '../world/WorldTypes';
export const distance=(a:WorldPoint,b:WorldPoint)=>Math.hypot(a.x-b.x,a.y-b.y);export const angleDifference=(a:number,b:number)=>Math.abs(Math.atan2(Math.sin(a-b),Math.cos(a-b)));const inside=(p:WorldPoint,r:Rect)=>p.x>=r.x&&p.x<=r.x+r.width&&p.y>=r.y&&p.y<=r.y+r.height;const segmentsIntersect=(a:WorldPoint,b:WorldPoint,c:WorldPoint,d:WorldPoint)=>{const cross=(p:WorldPoint,q:WorldPoint,r:WorldPoint)=>(q.x-p.x)*(r.y-p.y)-(q.y-p.y)*(r.x-p.x);return cross(a,b,c)*cross(a,b,d)<0&&cross(c,d,a)*cross(c,d,b)<0};export const hasLineOfSight=(a:WorldPoint,b:WorldPoint,blockers:Rect[])=>!blockers.some(r=>{if(inside(a,r)||inside(b,r))return false;const p=[{x:r.x,y:r.y,elevation:0},{x:r.x+r.width,y:r.y,elevation:0},{x:r.x+r.width,y:r.y+r.height,elevation:0},{x:r.x,y:r.y+r.height,elevation:0}];return p.some((q,i)=>segmentsIntersect(a,b,q,p[(i+1)%4]!))});export const canSee=(guard:EntityState,target:EntityState,blockers:Rect[],range=24,halfAngle=Math.PI/5)=>distance(guard.position,target.position)<=range&&angleDifference(guard.facing,Math.atan2(target.position.y-guard.position.y,target.position.x-guard.position.x))<=halfAngle&&hasLineOfSight(guard.position,target.position,blockers);
