/// <reference types="vite/client" />
declare global{const __BUILD_ID__:string;interface Window{readonly __GONE_TEST__?:{activeView:string;player:{x:number;y:number;elevation:number};playerScale:number;mission:object;loadedResources:boolean}}}
export {};
